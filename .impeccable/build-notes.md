# Build notes (dev only — never ship)

Request (Luca, 2026-09-23): "Based on the finding make a new website portfolio for Stefano and implement from the finding everything, follow a modern design."
"The finding" = the Awwwards SOTY page-1 teardown done 2026-09-22 (artifact https://claude.ai/artifact/GiTbTz7vyuebba92dfLB35, memory `soty-teardown-artifact`).

## Confirmed (see PRODUCT.md)
- Stack: Vite + three.js + GSAP. GSAP ticker is the ONLY rAF: drives Lenis (autoRaf false) and the WebGL render. matter/physics stepped from the same ticker if used.
- Audience: businesses needing a designer. Facts allowed: based in Albania (Albania-time clock ok, no city), EN + SQ, placeholder email hello@example.invalid. NO services list. No other clients/years/metrics.
- Evidence: elixir.al, barmartiri.com; screenshots in LC/claude-site/work/{elixir,martiri}-{desktop,phone}.jpg (copy into this project).
- Build path: code-first (.impeccable/config.json).
- Mode: Experience (portfolio), with a persuade-grade contact path.
- Do NOT touch LC/claude-site (another session's project).

## Findings to implement (checklist)
1. Reactive liquid cursor (Lusion): fluid velocity field post-pass, RGB/ink split.
2. Cursor reveal (Lando): dissolve mask around pointer revealing a hidden layer.
3. One 3D object riding the scroll (MANA): persistent object keyed per section.
4. Scroll-drawn line (Don't Board Me): SVG path stroke-dashoffset across sections.
5. Frozen in ice/glass + HUD labels (Igloo): transmission material + projected leader lines.
6. Grid-tile RGB glitch transition (Orano).
7. Pinned case exhibit (Noomo): giant word + object per project.
8. Outline-to-fill headline (Pioneer).
9. Circular portal / ring reveal intro (Kode, Bruno Simon) + odometer % (Lusion).
10. Chamfered frames + slit wipes (KPR).
11. Whole-site theme flip with copy rewrite (Other Side of Truth), @property cross-fade.
12. Sentence with inline pictures + word-by-word scroll highlight (Opal).
Also: two-typeface marquee + self-drawing flourish (Lando), fanned card arc (Lando), mixed serif/grotesk manifesto (Lando), card-dealing steps (Don't Board Me), stacked-card dropdown menu (Lusion), HUD live clock line (Star Atlas/Truth), grain (Nomadic Tribe), sliced display type + circular text badge (Synchronized), specimen hover card (Pangram), sticky pill toolbar (Opal/Pangram), sound toggle off by default (KPR/Orano), jump-game footer (MANA), joke 404 (Don't Board Me), loop back to top (Lusion/Igloo), font-from-drawing download (own idea from findings).

## Verification
Browser pane paints no WebGL. The tools use `puppeteer-core` (devDependency since 2026-09-24; before that they imported the chrome-devtools-mcp bundle from the npx cache, which breaks whenever that cache is cleared) with the installed Chrome, headless 'new', `--use-angle=d3d11 --enable-gpu --ignore-gpu-blocklist --mute-audio --disable-background-timer-throttling --disable-renderer-backgrounding` (see memory headless-chrome-for-webgl-sites). Captures to .impeccable/review/desktop.png (1440) + mobile.png (390).

## Flow status
- [x] impeccable context, init interview, PRODUCT.md
- [x] new-work: direction roll (seed 13d2bca7), decision page (user took Penny Broadside, code-led), surface brief with direction contract (.impeccable/surfaces/index-html.md)
- [x] craft-floor.md read before UI edits; emil-design-eng + design-taste-frontend loaded
- [x] build (2026-09-23): index.html, 404.html, src/ (ui/*, gl/*), fonts subset, favicon, band poster, og.png
- [x] inspect round 1 (desktop 1440 + mobile 390, headless GPU) -> batch fixed: fan inside margins, phone grid tracks minmax(0,1fr), side-block track, band land/sea overlap, stir strength
- [x] confirm round 2 + evidence refresh after adding the paper-feed slit (contract: "slit wipes as sheets feed")
- [x] detect --json: layout-transition fixed; flat-type-hierarchy is a false positive (sizes set at runtime by fit.js)
- [x] embed-prompt --scan public public/work: 10 rasters, 0 missing
- [x] finish review: fix (8 material) -> verdict pass 1: 3 resolved, 5 partial, 2 regressions (mottle) -> round 2 -> verdict pass 2: all resolved, disposition ship (scope: the scored fixes, not a new full review). Notes left: van body mostly line-shaded; mid-reveal word state not captured.
- [x] documenter: DESIGN.md + .impeccable/design.json written from the build; its two flags acted on (proof notes now ink text with red pencil underline; stylesheet header states where red is used); DESIGN.md axis range corrected to the 50..100 the fitter uses
- [x] optimize (2026-09-23, "some places are choppy"): see Performance below
- [x] red sheet lag (2026-09-23): GPU shader compiles on first sight, fixed. See "Red sheet" below
- [x] websites sheet (2026-09-23, Luca: "add website designer developer frontend backend, add coding languages that claude can do"): new sheet #services between About and EXTRA (head, lede, Design / Front end / Back end columns, the type case of 27 languages); role, masthead third line, stand, verse, marquee, contents, index, nav word, footer, title/description/og, JSON-LD, 404 lockup, all EN + SQ; og.png re-shot (band poster left alone). PRODUCT.md, DESIGN.md (+ frontmatter), design.json and the surface brief updated. Inspected at 390 / 768 / 820 / 900 / 1024 / 1100 / 1101 / 1280 / 1440 / 1920, EN and SQ: no overflow, the case tiles 12x6 above 1100px and 6x12 below.
- [x] fixes found on the way: phone sheet heads now take another line when that sets the smallest line a third larger (fit.js `grow`, head role only), where they used to sit on one line at 30px; accents on inked heads (the dots of Ë) were cut off by the ink window and by the fill's mask box, now covered (window from -0.5em, fill padded to match); "C++" pluses lifted to cap height.
- [x] the other sheets (2026-09-23, Luca chose from a list: case pages, how it was made, contact page + form, and /sq/): see "Sheets" below.
- [x] Dresses by Greta (2026-09-24, Luca: "add the website on my work, only the name and the preview, the domain will be bought later"): a third case sheet in the front page's stack, `case--preview`: the name fitted across the measure and two plates (greta-home, greta-phone, shot by tools/shoot-work.mjs with nothing clicked, provenance by tools/embed-plates.py without the temporary address), no facts, key, line or link. The work lede now counts "three sites, two of them open for business" (EN, SQ, and the index line). ink.js inks any case's name while its sheet is held; toolbar.js tracks the new sheet under Work. Checked at 1440x900, 1280x800, 820x1180, 390 and in Albanian: plates fit inside the held sheet, both go WebGL, no overflow, no console errors. Then (same day, Luca: "add greta to the fan and the contents too"): a fifth fan card, Greta on a phone, dealt in the middle so the arch stays symmetric (home page, phone, Greta's phone, phone, home page; three home pages and three phones cannot make a symmetric six), and a contents entry after Bar Martiri with the line "A first look" / "Një vështrim i parë" (it describes the sheet, not the business) and a thumbnail cropped on the sequined skirt, below the site's own wordmark. The footer still names only Elixir and Bar Martiri (there is no Greta page).
- [x] mobile interaction (2026-09-24, Luca: "think for mobile interaction and animation", then chose from a list: swipe the card fan, tap feedback + stamp, ink band + index drag; he did not take "hold to magnify"): on phones the fan is a deck (drag, flick at 0.11px/ms, rubber-band ends, tap front opens / tap behind brings forward, manicule buttons with a count, keyboard focus brings a proof forward); press states on the `scale` property for fan proofs, contents thumbnails, index sheets, words and the next line, plus an empty passive touchstart so iOS shows :active; the HANDED IN / RECEIVED stamp on the telegram (Albanian time, ink, speckled, lands before the mail app opens, 12ms buzz only with sound on); a tap on the band flicks the ink outward (splats queued by the event, made in the frame, over four frames); the index pulls down to close on phones (70px or a flick; springs back otherwise; a pull never opens a sheet). Found and fixed on the way: the deck's z-indices escaped the fan and painted over the index (the fan is now `isolation: isolate`); the stamp first sat on the addressee (phone) and on the word count (desktop), now placed from the strip's measured bottom; a pull's swallowed click could eat the next real tap (reset on every pointerdown); the Greta sheet asked GSAP to deal facts it does not have (5 warnings on phones, now skipped). Verified with scripted touch in headless Chrome at 390 (flicks, slow drags, taps, vertical scroll through the fan, index pull/flick/tap, stamp EN + SQ) and by capture at 390 and 1440; 20 page loads clean. Performance: front page on a phone with CPU x4 against the pre-change commit (9027edf, served from a throwaway worktree), two runs each: scroll frames over 20ms 18 / 12 before, 19 / 11 after: no change. The fan stretch (scrollY about 2000-3000 on a phone) was already slow in both: first-sight GPU compiles (FillRect twice, CircularRRect + FillRRect about 40ms at the fan) and frames of 33ms while the proofs are dealt; left for its own pass. `.impeccable/perf/redsheet-trace.json` (115 MB, rewritten by every tracer run) is now gitignored: GitHub refuses files over 100 MB.
- [x] finish review of the sheets (2026-09-23): fix first, 8 material fixes, all made in one batch: (1) callout rings sat on the details and their rules crossed out words: rings now beside the details, rules through clear space, placed from the live DOM (see Case plates); (2) "200 L" read as litres: ALL in English, lekë in Albanian; (3) up to 1100px the key had lost its letters: lettered list plus letters pinned on the plate; (4) holes at 1440 around phone plates: pairs run plate, key, plate, key and a lone phone plate is one centred cut; (5) the reply field's email keyboard: inputmode removed; (6) Albanian: pamje for pllaka, "tri lloje germash", the month names line, "Gjuha e përgjigjes", "dera kryesore e barit në internet", no comma before dhe (also in five front-page and services strings); (7) "sixty frames a second" replaced by what was measured, 95 frames in 100 within about 17 ms; (8) the colophon's empty ruled slot above the next sheet. With the new rings the Elixir list key reads "674 perfumes" before "Filter", to follow them down the plate. Verdict pass: 7 resolved; fix 3 partial with a regression (20px pins covered details on desktop shots at 390) -> pins step out from their details by their extra size and are 16px on phones, rules spaced 18px of the shot (about 11px on the page, from 7) -> rechecked at 390, 820 and 1440: the price reads "ALL 28,000" in full and no pin covers what it names. Not taken (ceiling notes): dealt fact cards beside the plates, a stamp on the handed telegram.
- [x] tablet case heads (bug found 2026-09-24): at 768 to 1100px the line, the button and the link shared the name's column beside the facts, so they ran on under the fact cards (at 768 the third card covered "The whole case") and the line wrapped into a 100px column. Now the facts sit beside the name only, at 14px so each card keeps to one line (the widest, "Dërgesë në gjithë Shqipërinë", is 246px of the 250px column), and the meta runs across the measure under both: in one row on a landscape screen, where the sheet is short (at 1024x768 the phone plate's caption had been cut off; it now ends 32px inside the sheet), and on a portrait one as the line, then the button and the link together. The name's column is the same either way, so turning the screen needs no refit. Checked on the build in headless Chrome at 768x1024, 820x1180, 1024x768, 1100x800 and 1440x900, EN and SQ: no overlaps, the link and the button are on top at their centres, no overflow, no console errors; with only the new rules removed from the live page, the layout at 390, 767, 1101, 1440 and 1920 is identical. Seen, not fixed (already there before): at 820 in Albanian the key label "Menuja dhe telefonata, krah për krah" runs under the phone plate; on portrait tablets the lower third of a held sheet is empty paper.
- [x] portrait tablets fill the held case sheets (2026-09-24, Luca: "fill the empty lower third on portrait tablets too"): side by side the plates are held to the width, which left 305 to 540px of bare paper under Elixir's and Bar Martiri's plates and 406 to 632px under Greta's. On a portrait tablet the case body is a size container (`container: case-body / size`) and, when its plate area is no more than 1.2 times as wide as tall, the plates are stacked and sized from 100cqh: the desktop plate at the left with its key to its right (270px kept for the key's longest line, the Albanian "Menuja dhe telefonata, krah për krah"), the phone plate under it a fifth taller, centred with its key on its left. cases.js needs no change: it sets each label from the live plate rects, the desktop key right of its plate and the phone key left of its plate. Greta (0.98, since its plates side by side are larger until then) sets both on the axis at one height. A shorter sheet keeps them side by side (checked at 800x900 and 768x800). Result, EN and SQ, 768x1024 to 1024x1366: the plates end 0.8px above the sheet's foot; at 820x1180 the desktop plate grows from 383 to 458px wide and the phone plate from 285 to 366px tall, Greta's from 337 to 402px tall; at 768x1024, the shortest, they stay the same size, spread over the sheet. No label over a plate (the Albanian label C no longer runs under the phone plate in portrait), none off the sheet, no overflow, no console errors; the next sheet still cuts the WebGL plates at its edge as it slides over. With only the new rules removed from the live page, the layout at 390, 767, 1024x768, 1100x800, 1101, 1440 and 1920 is identical. Labels in the stacked layout: cases.js sees the plates are stacked and gives each key its own side as room (see "tablet key labels" below), so the stacked block has no label rule of its own; a nowrap override added first was removed once that landed.
- [x] tablet key labels (bug found 2026-09-24): the front page's case keys were set on one line in the column between the plates (26px in from each plate), and at 768 to 1100px that column (175 to 254px) is narrower than the longer labels. In Albanian "Menuja dhe telefonata, krah për krah" ran 66px under the phone plate at 768, 54px at 820 and 5px at 1024x768, where the WebGL canvas drew the plate over it; in English "Menu and call, side by side" ran 2px under it at 768, and at 768 and 820 other labels in both languages ran into the gutters where the rules run. Now, at 768 to 1100px only, a label wraps inside its room (`.case__body > .callout`, `text-wrap: balance`, max-width `--callout-w`). cases.js measures each label at the start of its room (the column when the plates stand side by side; stacked, the measure on its own plate's side, so `--callout-w` is never negative), draws a wrapped box in to its longest line, sets a label left of its plate flush right so its rule meets the words, meets the first line with the rule, and pushes the next label down by the lines it adds (one-line labels keep their 32px). Checked on the build in headless Chrome, EN and SQ: 768x1024, 820x1180, 834x1194 and 1024x1366 (stacked: nothing needs to wrap); 800x900, 768x700, 900x700, 1024x768 and 1100x800 (side by side: up to three labels wrap per case). No label over a plate or outside its room, every rule 4px from its label and on its first line, no overlaps, no overflow, no console errors; switching language on a held sheet, and resizing between tablet, stacked and desktop, gives the same layout as a fresh load. At 1101, 1180, 1280 and 1440 every label box and rule path is identical to the build before the change; phones (a static list) and the case pages' keys are untouched. Then (same day, Luca: "apply the same wrap on desktop too") the rule applies from 768px up. A label that fits keeps its one line, so on desktops only Bar Martiri's Albanian C changes, from 1101 to about 1190px, where it had run into the gutter (20px at 1101, 6px short of the phone plate): it wraps in the column, and F, E and D under it move down by its extra line. Checked the same way at 1101x800, 1120x700, 1150x800, 1180x820, 1200x800, 1280x800, 1440x900 and 1920x1080, EN and SQ: all pass; against the tablet-only build only that case changed, and 1280 and 1440 are still identical to the original; switching language at 1101 and resizing across 1100 give the same layout as a fresh load.
- [x] fan stretch on phones (2026-09-24, Luca: the stretch where the fan comes into view, scrollY about 2000 to 3000 at 390 with CPU x4, to p95 about 17ms without changing the look): done, see Performance, "Fan stretch on phones". Not deployed.

## Sheets
Five sheets in two languages, ten pages. `npm run build` = `vite build` + `node tools/build-sq.mjs`.
- **Structure.** vite.config.js lists every page and runs a small plugin that inlines `<!-- @include x.html -->` from partials/ (head, defs, chrome = toolbar + pill + tab bar, dateline, foot, end = index + tiles + canvas + script). One main.js boots every sheet; each module does nothing where its elements are absent. The stage builds the 3D band and the ink fluid only where a band exists.
- **Addresses.** src/routes.js holds each sheet's English and Albanian address. Links carry `data-route` (and `data-hash` for a part of the front page) and are painted for the current language; a link to a part of the sheet you are on scrolls instead of reloading. The language switch re-sets the sheet in place under the tiles and moves the address to its counterpart (history.replaceState, built site only). A saved choice wins; the browser's language decides only on the front page's plain address (x-default).
- **Albanian pages.** tools/build-sq.mjs reads each built English page and writes its /sq/ twin: every data-i18n / data-i18n-list / data-i18n-attr text, the proof notes and the verse are replaced from STRINGS.sq, links marked data-route point at the Albanian sheets, and both twins get canonical, hreflang (en, sq, x-default) and og:locale. Without SITE_URL the links are root-relative and there is no sitemap: set SITE_URL for the launch build.
- **Case plates.** tools/shoot-work.mjs captures the live sites (desktop 1440x900, phone 390 at 2x), one fresh browser context per shot (a notice declined on one shot once hid the next shot's welcome card), declines their non-essential cookies, and places every callout from the page itself. It measures the detail's box (the control it sits in, a small chip around it, or the ink of its text) and every visible text line, control and picture, then sets the ring just outside the detail, on the side facing the key, at the height where a straight rule to the plate's edge crosses no word. Rules may graze leading and button padding, keep 18px of the shot apart (about 11px on the page) and stay in key order; where nothing beside a detail is clear the ring is pinned on its edge. Both sides are written to src/data/plates.json (`r`, `l`: `[x, y, dx, dy]`, the ring's centre and the way out from its detail), in key order: the finder list follows the `<li>`s, nothing is sorted. Up to 1100px the letters are pinned on the plate instead (folio.js), each stepped out from its detail by the size the pin has beyond the ring at that scale, so a desktop shot on a phone keeps its details uncovered. `--debug <dir>` saves each shot with the boxes, rings and rules drawn over it. Traps met: text that ignores the pointer (Elixir's notice bar) falls through `elementFromPoint`, so it is made hit-testable for the test; text inside faded containers (closed tooltips) is skipped; a site's CSP blocks the inline-styled debug overlay (the tool bypasses CSP in debug runs only), and an open `<dialog>` sits above any z-index (the overlay goes inside it).
- **Key sides and layout.** Elixir: Fig. 1 and 2 keys left, Fig. 3 right, the phone pair both right, Fig. 6 right. Bar Martiri: Fig. 1 right, the phone pair both left, Fig. 4 left, Fig. 5 and 6 right. Each side was the one whose rules cross nothing. A phone pair runs plate, key, plate, key (or the mirror), never key against key; a lone phone plate is set with its key as one 720px cut on the axis. The case pages' Fig. 1 plates (elixir-home, martiri-home) were shot on 23 September with the rest; the front page keeps its own elixir-desktop / martiri-desktop plates and anchors (src/ui/cases.js).
- **Provenance.** Every plate carries its origin note: after a re-shoot, `python tools/embed-plates.py` writes it (what was shot, when, how, and in what state). With the other session's icon set the scan reports 39 rasters, 0 missing (public/work is counted twice, inside public). The Bar Martiri basket plate has one vanilla in the basket of a throwaway browser; nothing was ordered or sent.
- **Contact.** src/ui/telegram.js: validation, word count, and a hand-off to the visitor's mail app (mailto, addressed from the page's [data-mail]); set ENDPOINT to post instead (Netlify Forms markers and a honeypot are already on the form). The privacy note says what the site stores; change it with the mode.
- **Verified** on the built site: the ten pages load with no console errors; titles, html lang, nav routes, canonical and hreflang are right in both languages; 6/6 plates go WebGL on each case page; no overflow at 390-1920; the telegram's empty and full sends behave; the switch on an inner sheet moves the address and a reload serves the Albanian twin; profiles: Elixir and colophon pages p95 16.9 ms or less, 0 long frames on desktop; Bar Martiri at 390 with CPU x4 p95 17 ms or less, 0 long frames; front page unchanged (capture 62 fps, no overflow).

## Performance
Measure on the production build (`npm run build`, launch entry "stefano-doko-preview", port 3671): `node tools/profile.mjs [--mobile] [--cpu 4] --label name` scrolls with real wheel events, stirs the band, hovers a plate, and records frame intervals and long animation frames. `tools/trace.mjs` (Chrome trace of a scroll stretch) and `tools/cpu.mjs` (CPU profile) go deeper. Runs are saved in `.impeccable/perf/`.
- Before (`before-desktop.json`): 16 long frames (about 1 s in total); the first 2000px ran at p95 50ms with frames up to 150ms; the case sheets ran at p50 33ms (30 fps).
- After (`ship-desktop.json`, `ship-mobile-cpu4.json`): p95 16.8ms in every stretch, 0 long frames on desktop; phone at 390px, DPR 2, CPU slowed 4x: p95 at most 17.4ms, 1 long frame.
- The 50ms frame at the red EXTRA sheet was first thought to be raster of the red layer and left; it is fixed now, see "Red sheet".
What fixed it, in order of weight:
1. The paper textures were megabyte `data:` URLs held in custom properties (--grain, --mottle, --speckle); every style recalc re-parsed them. paper.js now hands them over as `blob:` URLs.
2. One read phase, then one write phase per tick (main.js: editionMeasure, nav.measure, measureInk, stage.measure, then the writes), so the page lays out once per frame instead of being forced to many times.
3. Styles written only when their value changes (toolbar pill/read line/aria-current, case leaders, labels, facts, veil).
4. Composited motion: the ink reveal is a sliding window of two counter-translated layers (no per-frame clip-path); the case veil is its own element animating opacity (was an inherited custom property); the red sheet's slit is a one-shot CSS transition when it enters (was scrubbed); the fan's transitions only run once it is dealt.
5. WebGL: the band's static meshes merged by material (about 370 draw calls to 73), the band re-cut at 30 fps unless the pointer works it, nothing redrawn when nothing changed (scroll, stir, plates, brayer, forced on resize/refit/lang/proof), shader programs compiled up front against the render targets they are used with, plate textures uploaded in idle time, DPR capped at 1.5 on desktop, fluid pressure solve 16 to 10 iterations.

### Red sheet
`node tools/redsheet.mjs [--at selector] [--mobile] [--cpu 4] [--w 1920 --h 960] [--runs 2] [--css "..."] [--profile DIR] [--invalidations]` walks the page down to just above a part of it (the red sheet by default), then records its first sight: frame intervals, every GPU shader compile with the Skia op that asked for it and the scroll position, the longest GPU tasks, main-thread paint/layerize totals, and (with `--invalidations`) which elements repaint and why. Each run is a first visit (empty shader cache) unless `--profile` keeps a profile.
- Cause: not raster. Chrome's GPU process compiled Skia shader programs that nothing earlier on the page had needed (28 to 33ms each, on the GPU main thread, so the frame waits): 8 to 9 compiles, 114 to 151ms, one 50ms frame as the sheet's top entered prepaint at 1920x960.
- Who asked: the About verse's two inline cuts, opened by an animated `clip-path: inset(... round 6px)` (a rounded clip is a program variant no other element uses); the EXTRA stamp, an exact circle (`border-radius: 50%`) with inset rings and a soft shadow (circle-only blur and ring programs); and what was left only compiled with the stamp and the outlined EXTRA head both present (hiding either removed it; no single part of the head was the trigger).
- Fixes: the cuts open by transform and opacity on their own layer (scaleY 0.12 to 1); the stamp's radius is 99px (reads round, draws as a rounded rectangle) and it has its own layer; and a copy of the red sheet is drawn once under the intro and dropped before the stamp opens (`intro.covering()` / `intro.onOpen()`; it was `warmExtra()` in extra.js, since 2026-09-24 `warmSheets()` in src/ui/warm.js, which also draws the fan and a plate, see "Fan stretch on phones"), so whatever it compiles, it compiles while the counter hides the page. Not done under reduced motion (no intro to hide it).
- After: first visit, red sheet entry: no raster compiles, three 6ms compositor compiles, worst frame 18 to 20ms. Returning visit (Chrome keeps shaders on disk): 9 cache loads, 3ms, no slow frames. Full profiles (`redfix-*`, `services-*`): desktop p95 16.8ms everywhere, 0 long frames; phone at CPU x4 p95 16.8ms, 0 to 2 long frames per run, which move between runs.
- Machine load matters: with the other session rendering in Chrome, the phone runs swung from 3 to 22 slow frames in the same stretch. Compare runs made back to back, and repeat before believing a regression.

### Fan stretch on phones (2026-09-24)
At 390 (DPR 2, mobile) with CPU x4, the stretch where the fan comes into view (scrollY about 2000 to 3000) had 3 to 11 frames over 20ms per run and p95 33ms, the same on 9027edf. Traced with a copy of profile.mjs's phone scroll that also records a Chrome trace (Layerize and PaintArtifactCompositor::Update per frame, paint invalidations with their reasons) and a CPU profile of an unminified build. The slow frames were main-thread frames, about 16 to 19ms of work each at CPU x4 during the deal (Layerize 5.9, the tick 6.1, style 4.0, paint 3.4), plus five first-sight shader compiles.
1. The deal repainted the cards every frame. fan.js wrote --x/--y/--rot/--s on each card, and Chrome repainted all five cards' paper (`var(--stock)`, invalidation reason "background") and re-rastered them on every frame of the deal. Writing the transform whole was not enough: with `var(--lift)` in it the paper still repainted, and without any var() an exact copy of the card still did (a bare div with the same paper did not; the difference was not isolated). Fix: the paper, padding, ring and shadow are on an inner `.fan__sheet` (index.html), whose style never changes; the card only moves. fan.js writes each card's whole transform, only when it changed; the hover's lift is the `translate` property (applied outside the transform, the same 28px, with the same 500ms once dealt); the card keeps `border-radius: 10px` and `box-shadow: none` so the global :focus-visible ring looks as before.
2. Every tick forced a second style pass. GSAP renders its tweens (the deal's scrub, the flourish's strokes) before ticker listeners added after it, so the tick's first read (`editionMeasure`) paid a forced style recalc on every frame of the deal: 89ms of 1244ms busy over the stretch. The one clock is now first on the ticker (`gsap.ticker.add(fn, false, true)` in main.js): it reads before any tween writes, and the tweens render after it in the same frame. Forced layout in the read phase fell from 89 to 19ms over the stretch, the tick from 132 to 41ms.
3. First-sight shader compiles. The fan's screenshots compiled two FillRect programs as the deal began (15ms each), and the first case plate's mount compiled CircularRRect + FillRRect (about 40ms of GPU at scrollY 2830; the chamfered clip-path on the rounded mount, not the fan). `warmExtra()` became `warmSheets()` in src/ui/warm.js: under the intro it draws the red sheet (as before) and, in a band from 36vh (clear of the red sheet's head and stamp at 390 and 1440), a copy of the fan and of the first plate. The fan copy needed two cards drawn at scale 1.1 (a copy lying in the pile at scale 1 left both compiles, on desktop too) and its pictures decoded synchronously (without, one compile came back in one run of two). The copy lives about 0.7s (desktop) to 0.95s (phone); the counter was already frozen for 0.5 to 1s during boot in both builds, and the copies add one or two slow frames there and none to its opening.
- Measured on the build (phone runs with CPU x4), against the same working tree without this change served from a copy, run back to back. profile.mjs, five pairs: scroll 2000 p95 17.1 / 33.4 / 33.2 / 33.3 / 16.8 (frames over 20ms 1 / 9 / 5 / 4 / 0) to 16.8 in all five (2 / 0 / 1 / 1 / 1); scroll 3000 16.8 (1 / 1 / 2 / 1 / 0) to 16.7 to 17.0 (0 / 0 / 0 / 2 / 1); frames over 20ms over the whole scroll 38 to 30. Files: `fan-after.json`, `fan-after-2..5.json`, `fan-before-1..5.json`. The scroll 8000 and 9000 stretches trade slow frames from run to run (together 14 before, 17 after; in three rounds of the scroll to 9700, 10 before and 6 after). Long frames over 50ms: none in the five runs before; 2 and 3 in two runs after (69 and 76ms at scrollY about 8650; 50, 50 and 63ms at 900, 3420 and 6000), no script and about 1ms of render; the tracer finds the same compiles at the type case and the red sheet in both builds, and earlier saved phone runs have 0 to 1 per run. Not traced to a cause.
- Compiles at the fan (redsheet.mjs --at "[data-fan]"): phone, before TextureOp(viz) 6, FillRect 15 + 15, CircularRRect 9 + FillRRect 30ms (a 42 to 44ms GPU task at 2830); after none, in three runs of three. Desktop, before 7 compiles, after 1 (FillRect about 10ms at scrollY 1015: not the fan, it stays with the fan hidden, and it was there before). The type case and the red sheet: the same compiles before and after.
- Desktop (profile.mjs, two pairs): p95 16.7 to 16.8 in every phase in all four runs; frames over 20ms per run 4 and 2 before, 2 and 2 after; one long frame (52ms) in a run before, none after. Files: `fan-desktop.json`, `fan-desktop-2.json`, `fan-desktop-before-1..2.json`.
- The look: screenshots of both builds compared pixel by pixel (phone half dealt, dealt, and one proof on; desktop half dealt, dealt, hover and keyboard focus): the phone deal identical, elsewhere 40 to 87 pixels differing by at most 3 in 765. The deck's behaviour (flicks, slow drags, the hands, vertical scroll, taps behind and in front, the way back into the pile) and the desktop hover are identical on the scripted touch test, as are /sq/ and reduced motion; no console errors.
- Not done: the marquee still writes its transform every frame it is on screen (scrollY 1515 to 2659 on a phone), and in Chrome any transform written from script costs a full compositor update (PaintArtifactCompositor::Update, about 5ms at CPU x4 on this page) on that frame; a WAAPI or CSS animation does not. Run on the compositor it would free those frames, but its speed follows the scroll's velocity, which a compositor animation cannot. img.decode() ahead of sight and smaller sources on phones were not needed: four of the five screenshots are loaded by the contents and the feature about 0.2s into the visit, and no image decode showed up in the stretch's slow frames.

## Traps met
- Grid `1fr` tracks grow to the unfitted text / 1440px screenshots before fit.js runs -> the phone layout viewport went 1017px wide. Every grid that holds fitted type or plates uses minmax(0, 1fr).
- Element screenshots (puppeteer) hang on the live canvas; use page.screenshot with clip + captureBeyondViewport:false (tools/stills.mjs).
- Chrome ships without Albanian month/day names: albaniaNow() sets them by hand.
- WebGL draws above the DOM (one canvas, z 30; fixed to the screen from 768px up, part of the page on phones, see "iPhone" below): anything that must sit on top of a plate is drawn in the plate shader (callout rings and rules); proof notes carry z-index 35; the next case sheet clips the plates and the brayer by scissor.
- A `data:` URL inside a CSS custom property is re-parsed on every style recalc that touches it. Big generated textures go in as `blob:` URLs.
- three.js compiles a separate program for a render target and for the screen. Warming a pass against the screen still left a 45ms compile on the first stir; warm it with the render target it really draws into set.
- The frame skip needs every visual input in its dirty check. The roller's show/hide was missing at first. Switching to PROOF without scrolling hides the roller but moves nothing, so the brayer stayed drawn. The check now includes `roller.visible !== lastRV`, and a proof switch forces two redraws.
- A first-visit hitch with no main-thread work in it (long frame, render 3ms, no script) is the GPU process: look for `shader_compile` in the trace. Rounded clip-paths, exact circles with rings or blurred shadows, and new combinations of features in one tile each need their own Skia program.
- `.sort` is the 404's pied type. The languages' case uses `.typecase*`, and DESIGN.md's "type-case tiles" is the language-switch effect, a third thing.
- The ruflo-core plugin's after-Bash hook re-runs the text of Bash commands through cmd.exe (via an npx shim with shell:true), splitting at `&&`. It emptied tools/redsheet.mjs after a command that ran `sed -i` on it, and left stray files named like `name.html;` in LC/. Run commands here with the PowerShell tool, which that hook does not watch.
- Cross-document view transitions (the sheet feed between pages) were aborted at random with "InvalidStateError ... ViewTransition opt-in disabled": the new page sometimes revealed before its external stylesheet's `@view-transition` rule counted. The opt-in is now also written inline in partials/head.html; since then every page-to-page move transitions.
- Another Claude session works in this folder too (print, brand, the logo in partials/defs.html and chrome.html, and a block at the very end of style.css). Re-read shared files before editing, use targeted edits, and tell each other before touching the other's files.
- A PowerShell hook refuses any command that mixes Remove-Item with a forward slash anywhere in it; keep deletions in their own command with backslash paths.
- In Chrome, a transform written from script (translate, translate3d, rotate, scale, or the individual properties) makes that frame run a full compositor update (PaintArtifactCompositor::Update, "Layerize" in a trace); a WAAPI or CSS animation does not. On this page it costs about 5ms per frame at CPU x4, so every frame with the marquee on screen pays it.
- A box whose style changes every frame repaints a `var(--stock)` background every frame when its transform is written through custom properties or var(), and the fan card did even without them. Keep the paper on a child whose style never changes and move the parent.
- GSAP renders its tweens before ticker listeners added after it. The one clock is added first (`gsap.ticker.add(fn, false, true)`) so its read phase comes before any tween writes. Move it back and every frame with a running tween pays a forced style recalc in `editionMeasure`.
- A throwaway copy under the intro only warms what it draws inside the screen (one fixed box: the red sheet, with the fan and a plate in a band over its middle, clear of its head and stamp), and the fan's pictures needed cards drawn at another scale than the real pile's. Check with redsheet.mjs that the compiles are gone, in more than one run.
- Git Bash's grep, sed and od here hide carriage returns: src/style.css is CRLF (in HEAD too). Count line endings with node before building a text patch.
- `.letter` is the letter-cutting game's class. The client quotes first used it too, so the game's grid laid out the quotes and the quotes' rules landed on the game. They are `.reader*` now. Grep a class name across src/ and the HTML before giving it to something new.
- The `hidden` attribute loses to any author `display`: `.btn`, `.plate` and `.case__meta` all set one, so a switched-off WhatsApp button, portrait frame or visit link still showed. Every optional part is covered by one `[hidden] { display: none !important }` rule, scoped to the parts' data attributes (the admin has a global one).
- The print stylesheet hides every child of `<body>` but the broadside, and fit.js measures type in a hidden span appended to `<body>`. A broadside built while print styles applied (on `beforeprint`) measured 0px and set the masthead at its 400px cap. The meter is `.fit-meter` and the print rule spares it.
- In the admin's four-column rows, a hidden chip leaves the grid, and auto-placement moved the switch into the chip's 12rem column and stretched it. Items are pinned to their columns above 760px.
- `wrangler dev` serves the `dist/` it found at start: after a rebuild, restart it. A ` ` written in tool input became the raw character and broke a regex in worker/inject.js, so the Worker never started; build such characters with String.fromCharCode.
- Every sheet without the band logged 12 WebGL warnings ("GL_INVALID_VALUE: glGetProgramiv: Program object expected"), the front page none (784e93c's build too). `warm()` in src/gl/stage.js links the plates' program through a probe plate, and it disposed the probe's material straight after `renderer.compile()`, so three.js deleted the program within a millisecond of starting its link. three.js turns on KHR_parallel_shader_compile, and with it Chrome's GPU process checks a program's link status until the link is done: it found the program gone, and each check logged the warning. No script touched the program again (every program call wrapped in the page: none reached a deleted one), and with the delete skipped, or the extension hidden from three.js, there were no warnings. The front page escaped only because the fluid's eight passes are linked right after the probe: without `fluid.warm()` it logged the same 12. The probe's material is now kept, never disposed, and the warm-up now reaches the plates: they share its program, where before they linked a copy of their own on their first frame (program #5, 82ms into /work/elixir/). Checked on the build: 0 warnings on all ten sheets and the 404, desktop and phone; plates pixel-identical before and after (on a case page only the live clock differed); profile.mjs, before and after run back to back (/contact/ ×3, /work/elixir/ ×2 on desktop and ×2 on a phone at CPU x4, / ×2): p95 16.8 or better in every phase of every run after; frames over 20ms, before and after, 0 and 0 on /contact/, 1 and 0 on /work/elixir/, 1 and 0 on the phone, 12 and 7 on / (the ~33ms frames at scrollY 2000, 4000 and 6000 are in both builds). The two runs on port 3671 are `glwarn-before-contact.json` and `glwarn-after-contact.json`.
- Between 768 and about 1130px the toolbar's lockup wrapped (Luca saw it: "the line is overlapping"): the name onto two lines and the descriptor word by word, so the lockup grew taller than the 78px strip and ran over the double rule and the bottom rule onto the dateline's date, and the end cell, stretched to the taller row, drew its rule down through the clock and the words; up to about 1200px the descriptor alone wrapped. At full spacing the parts need about 1110px (lockup 330, words 337, language and ink button 380, two 28px gaps), and a grid's `auto` track is the first to give way, down to its min-content. The earlier inspections looked for overflow, which this never makes (nothing scrolls): count a text's line boxes and test them against the rules instead. The toolbar now closes up in steps (style.css, before the 768 to 1100px block; DESIGN.md, Toolbar): 16px spacing up to 1240px, the phone descriptor and no hand up to 1100px, no ink button up to 1000px. Swept every 2px from 1240 to 768 on the front page, contact, a case and how-it-was-made, EN and SQ, with a scrollbar and without: the lockup keeps its two lines and nothing crosses a rule, with at least 12px to spare beside a 10px scrollbar.

## The admin and the optional parts (2026-09-24)

Built at Luca's request ("add everything, activated from an admin panel"), on Cloudflare (Worker + KV, as LC/greta), admin in English and Albanian. Not deployed and not committed. Checked with a scratch headless run (every part on through the admin API, desktop and phone, EN and SQ, print to PDF, the masthead, the phone loupe with real touch events, the inbox), `npm run test:admin` (46 checks), axe-core on the public pages and every admin panel, redsheet on the rate card (no frame over 20ms, desktop or phone) and profile.mjs with everything off against touch-*.json (no regression). Each run put the local settings back as it found them.

## Toward Site of the Day (2026-09-24)

Luca asked what else would beat the other entries, then "do it all". Measured first with the awwwards-blueprints skill (fingerprint.mjs, audit.mjs): the site already beat the portfolio winners on weight (1.4 MB against an 8 MB median), one animation loop, layout shift, skip link, reduced-motion CSS and alt text, so no new effects: the work went into the jury's weakest criteria (usability 30%, accessibility the lowest developer score) and into content.

- Stop the press: a date-line word that sets `state.reduced` (every self-moving part reads it each frame), inks every head, keeps the clock to the minute, drops Lenis's glide (`lerp` 1), and is kept in localStorage (`sd-still`; partials/head.html sets `.no-motion` before first paint, so the next sheet opens still). The privacy notes list it with the other stored choices.
- Availability: a ninth optional part, a month in the admin (Contact), shown as one CV line on the front page, the contact page and the print edition; "now" once that month has come in Albania.
- Structured data per sheet and language from build-sq.mjs; the Worker adds the email and number to the same Person once they are live. `npm run test:admin` now runs 51 checks.
- The hero is the page's largest paint: the band poster has `fetchpriority="high"`, and warm.js leaves the picture out of its plate copy (with the stage on the real plate's picture is drawn in WebGL anyway). Before, the full-width copy of the first plate, lazy, was the LCP at 2.1s instead of the band at 0.75s. A/B with redsheet at the first plate, two runs each, desktop: the same three first-sight compiles either way (CircularRRectOp 12ms, AtlasTextOp 8 and 11ms: not the plate's, and still open for warm.js), frames over 34ms 0 with the change against 1 per run without; phone: none over 20ms.
- Hit areas grown to 24px (44px under a finger) without moving anything: padding plus an equal negative margin.

Traps:
- The print edition's word was "Print", and PROOF mode renames the proof word "Print" (the way back): two "Print" words side by side. The edition's is "Print a copy" now, and it stays off phones.
- An audit's focus check calls a control "covered" when `elementFromPoint` finds another element on top: the intro's opening ring is a mask, which does not stop the counter catching the pointer. The counter lets pointer and focus through once it starts to open.
- The skill's audit finds a stop switch only by its name ("stop animation", "animations off"): the switch is named "Stop the press: stop animations" (it starts with its visible words, so it still passes label-in-name).
- Its contrast check reads the colour from the element's ancestors, so the band's caption, which sits below the band on paper, is measured against the band's red (4.30:1). axe measures it on the paper and passes it; nothing was changed for it.
- A later rule of the same weight wins even over a media query: `.specimen { padding }` further down the file undid a `(pointer: coarse)` rule for it until that rule was written `button.specimen`.

## iPhone: lag, spacing and the Albanian (2026-09-25)

Luca, on an iPhone 17 Air: scrolling the hero and the page previews lagged, the spacing of the writing was wrong, and the Albanian had words that made no sense together.

- The lag: Safari on the iPhone scrolls the page itself, between the script's frames, and the one WebGL canvas was fixed to the screen. What it drew (the band, the plates, the brayer) stayed where the page had been until the next frame: the band and the screenshots trailed the finger. Shown in Chrome by stopping the page's clock (Playwright's `page.clock`; overwriting `window.requestAnimationFrame` does not stop GSAP, which keeps its own reference) and scrolling 200px: with a fixed canvas the engraving hung 200px below its box over the text. On phones (`state.mobile`) the canvas is now part of the page (`onPage` in src/gl/stage.js): a `.gl-page` holder as tall as the page (`body.offsetHeight`, which an absolutely placed child does not change) clips it, the canvas covers the screen and 0.3 of the screen's height above and below, sized for `max(innerHeight, screen.height)` so the address bar never re-sizes it mid-scroll, and is set back over the screen (a transform, whole pixels, kept within the page) once the page has carried it half that margin, about every 180px. Everything is measured on screen and drawn at `rect - cTop`; GL rows count from the canvas's bottom (`glY`); the band's wet ink reads the fluid through `uVelRect`, the screen's place in the drawing buffer; the brayer is drawn into that same screen rectangle with its camera and outline width unchanged. A scroll alone no longer redraws on phones (what was drawn moves with the page). Same test after: the engraving stays in its box. Wider screens keep the fixed canvas: Lenis moves their page in the frame that draws, and their case sheets are sticky while read, which a canvas scrolled with the page cannot follow (so an iPad still has the old behaviour). profile.mjs at 390px, CPU x4, two runs: worst p95 16.8 and 16.9ms, 4 and 3 frames over 20ms, none over 34 (admin-off-mobile-cpu4: 33.3ms, 9 and 1). Desktop, two runs: p95 16.9, and its few slow frames fell in different places each run.
- The fan's deal was smoothed (`scrub: 0.6`) on top of the phone's own scroll glide, so the proofs trailed the page: under a finger it follows the scroll directly (`scrub: true`).
- Spacing: on phones the stand's two proof notes were `display: block` while hidden, which left a 38px and a 64px blank under the stand's head and under its buttons (they now show only in proof). The Albanian "Shkruaj" filled its equal fifth of the tab bar to its edges: the ink button now has 1.4 of the bar (the language toggle 0.8), and a smaller hand under 400px. The stamp's ring words were longer than their half of the ring (253 and 232 units against about 196 between the stars) and ran into the stars in both languages: extra.js sets both to the smaller fit (`getComputedTextLength` against the arc). The game drew its caption with `fillText`'s maxWidth, which squeezes the letters together: it wraps now. The longer Albanian nav words ("Punimet", "Shërbimet") wrapped the toolbar's lockup at 768-786, 1002-1020 and 1102-1122px: under `:lang(sq)` each closing-up step comes about 30px sooner and the nav closes to 12px up to 800px (swept every 2px, 768-1440, both languages, toolbar and pill: clean).
- Playwright's WebKit on Windows (the WinCairo port) lays text out with a variable font's axes but draws every glyph from the default instance: Anybody set at 900/70% draws as a hairline on heavy spacing, which looks like broken letter spacing. It cannot show what Safari on an iPhone draws; use it for layout, not type.
- The Albanian was rewritten as Albanian (src/i18n.js sq, the admin's words too). The newspaper images are kept where they read naturally (ballina, "Lajm i fundit!", telegram) and said plainly where a literal one meant nothing: "Stop the press" is "Ndalo lëvizjen" / "Lëvizja u ndal", Proof is "Korrektura" (its way back "Botimi"), Sound off/on "Pa zë" / "Me zë" ("Zëri fikur" lacked its article), a live site is "online" (not "e gjallë"), the engraving a "gravurë", "I gjithë rasti" (a case, as an incident) "Projekti i plotë", "janë hapur sot" (have opened today) "janë të hapura sot", shezlone "shezlongë", "Cut your own letter" "Krijo shkronjën tënde", the nav "Punimet". The front page's picture descriptions and the typeface cards were English on /sq/: they have keys now (featureAlt, elixirAlt2, martiriAlt2, gretaAlt1/2, specAnybody/specUltra/specOS, typefacesLabel). A scratch check keeps the two dictionaries' keys, kinds and list lengths the same. The verse's hand hyphenation follows the new words.

## Everything that could be added (2026-09-25)

Luca asked what else could be added, then "do it all". Built here: the Italian version, per-sheet link previews, five new optional parts, and the iPhone's smoothness on tablets. What needs Luca or Stefano (domain, deploy, password, real graphic work with permission, profile addresses, the Web Analytics token, a native reading of the Italian) has its place in the admin, waiting. Not deployed and not committed.

- Italian: `STRINGS.it` in src/i18n.js (key for key with en and sq; the scratch check compares keys, kinds and list lengths), /it/ addresses in src/routes.js, written by tools/build-sq.mjs with hreflang en/sq/it + x-default and `og:locale` it_IT. The Person's `knowsLanguage` stays sq and en, and the telegram still asks for Albanian or English (Stefano is not said to speak Italian). The date is it-IT from Intl; the verse has Italian hand hyphenation. Toolbar: an IT key; phones: the tab bar's language word opens the three names (`.langpop`, inside the tab bar's nav so it is in a landmark).
- Link previews: tools/og.mjs renders 15 (5 sheets x 3 languages) at 1200x630 from the sheet's own words, type and picture; build-sq.mjs points each sheet's `og:image` at its own. Each has its origin note (`impeccable embed-prompt`; `--scan` 73 rasters, 0 missing).
- New parts (14 in all): profile links (`rel="me"`, `sameAs`), save to contacts (shared/vcard.js for both the Worker's /stefano-doko.vcf and the page's QR; the admin downloads the QR as an SVG for print, full 4-module quiet zone, EC M; on screen EC L at 176px, so the long card fits fewer, larger squares), the price calculator (under the rate card; its list goes into the telegram through `?rates=`), graphic work (photos uploaded in the admin, kept at once, dropped with their piece; plates that develop like the case plates) and visit counts (the Worker adds Cloudflare's beacon; the privacy notes change wording). Admin: graphic and visits panels, EN/SQ/IT boxes with the Italian one lighter (may stay empty: the English shows), and a note on the overview saying so.
- iPad: a tablet scrolls the page itself, as the iPhone does, so the page canvas is on the page wherever the pointer is not fine (`onPage = state.mobile || !state.fine`). Its held case sheets are sticky, which a canvas on the page cannot follow, so each case sheet gets its own canvas inside it (`.gl-case`, a WebGLRenderer of its own, no MSAA, at the screen's density up to 2, depth for the brayer): the sheet's plates and the brayer on its name are drawn there, only when they change, and the next sheet covers it as paper. No occluder scissor is needed on them. A sheet out of sight (off screen, or with the next one laid square over it) releases its drawing buffer after 2s and redraws on return (within half a screen), so at most two hold one. Frozen-clock test at 820x1180 (touch): the band after a 220px scroll, Elixir held and scrolled 200px, the brayer mid-roll, Bar Martiri sliding over Elixir: every drawing stays on its box. Native smooth scroll down the stack and back: 1788 frames, p50 16.7, p99 16.8, max 50ms, one frame over 34; the phone profile: max 16.8. Desktop keeps the fixed canvas (no sheets); phone: page canvas, no sheets; no console errors on any.
- Toolbar with three language keys: the lockup wrapped again in all three languages. Steps now 1250 / 1136 / 1030px, a new one at 850px (gaps 12, mark 46px, descriptor 0.1em), Albanian about 45px sooner and its words at 10px up to 790px. Swept every 2px from 768 to 1440 in the three languages, toolbar and pill: clean.
- Also: the stored-choice rule means a visitor who chose Shqip opens /it/ in Albanian (and the address moves to /sq/); the calculator's sum is set in the lines' case ("from", not "FROM"), prices 19px on phones; "Haskell" in the type case ran 2px out of its box at 375px in every language (plain names at 76% width under 400px); the QR frame's corner and the admin's language chips moved onto the 4/6/10px scale.

Checks: `npm run test:admin` 72/72 (settings put back); axe-core with all 14 parts on (front page EN/IT desktop, IT/SQ phone with the language names open, contact EN desktop and IT phone, admin overview/contact/rates/graphic/visits, graphic on a phone): only the known fan-proof target on phones; phone-fit at 420 and 375 on the five Italian sheets: clean; no English or Albanian left on the Italian sheets beyond names, loanwords (home page, front end, online) and the Albanian letters the specimens show on purpose.

Traps:
- `vite preview` (3671) serves `dist/`, not the source: a change shows there only after `npm run build`. The worker (3672) picked up the new build's files without a restart this time; restart it anyway after a Worker code change.
- A fixed element placed with `left: 50%` gets only half the screen to shrink into, so the pill was set at its min-content width and "Chi è", the one two-word item, broke at its space in almost every width. Section words are `white-space: nowrap` now.
- The mark's own rules come after the toolbar's media queries in style.css (another session's block), and a later rule of the same weight wins over a media query: the 850px step scopes its selectors under `.toolbar`.
- Chrome's `Input.synthesizeScrollGesture` did not scroll this page under Playwright (scrollY stayed 0), and a loop waiting for it hung the tab until it crashed. `scrollBy({ behavior: 'smooth' })` gives a browser-driven scroll instead.
- In Git Bash, arguments like `/it/` are turned into Windows paths (`C:/Program Files/Git/it/`): set `MSYS_NO_PATHCONV=1`.

## The sun over Albania, and the tilt (2026-09-25)

Luca asked for "something new" twice after two lists of options, so one thing was chosen and built: the engraving follows the real sun, and a phone's tilt moves its light. Local only, not committed or deployed.

- src/sun.js: NOAA's solar position for the middle of Albania (41.15 N, 20.17 E; the site names no city). Checked for 25 September 2026: solar noon 12:31 at 47.6 degrees, sunrise and sunset about 06:31 and 18:31 (CEST); 72.3 degrees at noon on the summer solstice, 25.4 in winter. `?sun=HH:MM` sets the hour (Albania time, today) for stills and checks.
- src/gl/band.js `setSun`: the sun group is dynamic now (its ring is its own mesh, one more draw). Bearing 60 to 300 degrees runs it across 28 units at z -22; height from the horizon, which on the beach's side is the sea's far edge (-0.26 at the sun's distance) and on the perfumes' the nearer end of the land (-1.65): without that second horizon the dawn sun floated above the land. Between 2 and -1 degrees the disc sinks by its own radius (half into the sea at 0.5), below -1 it has set. The lamp comes from the sun's side; at night a low front lamp. In the two-panel print (narrow screens) both panels see the middle of the sky, which drew two suns (the old fixed sun was already drawn twice on phones): `aim(panel)` now draws it in one, the perfumes' before noon and the beach's after.
- src/gl/stage.js: the sun is set every 30s with a forced redraw (a frame-accumulator shortcut would have redrawn every frame under reduced motion). `lean(dt)` reads `state.tilt` (src/ui/tilt.js only notes the reading): the hold drifts to the way the phone is held over about 4s, the lean is (reading - hold) / 20 degrees, smoothed; while it moves the band redraws every frame, as under a pointer. The view leans 0.7 of the pointer's parallax.
- iPhones and iPads need `DeviceOrientationEvent.requestPermission()` from a press, so a word under the band asks ("Tilt to move the light"), 46px tall, and goes once answered. Chrome here also has the function, so the word shows on any touch screen; nothing is asked until it is pressed.
- The band's caption went into the flow (`figure.band-fig` > `div.band[data-gl="band"]` + `figcaption`): absolutely placed, its new sun line and the word ran over the next section's rule on phones. `.front` lost the room it kept for a one-line caption (38 to 12px, 52 to 16px on phones), so a one-line caption on a desktop sits where it did. A right-aligned `fit-content` caption needs a `max-width`, or at 820 it ran past the band's left edge. The word's hit area (negative margins under a finger) pulled the next rule up to 2px: its margins are set.
- The date line's three words wrapped at 320px in Italian (reported by the other session): nowrap, and at 360px and under 11px type, 0.03em, 10px gaps. Checked at 320, 340, 360 and 375 in three languages: one line each, at least 30px apart.

Checked: sunrise, morning, noon, afternoon, the setting sun (half in the sea behind Bar Martiri's kiosk) and night on a desktop and a phone, one sun per panel; a CDP orientation override on a phone leans the light and the view and they settle back within six seconds; the iPhone path with a stubbed prompt (the word shows, the press asks once, the word goes); the caption at 1440, 820, 390 and 320 in three languages (12px or 16px to the rule below); axe with the word showing (phone IT, tablet SQ): only the known fan finding; frame times at 390x844 max 16.9ms, at 820x1180 max 34ms over two runs (one run had a single 66ms script frame that did not recur); no console errors.

## The night edition: the sun turns to the moon (2026-09-25)

Luca: "make it so when i click the sun it will change to a moon and make the website dark mode". Local only, not committed or deployed.

- The switch: the engraving's sun is a real button (`button.band__sky[data-sky]` inside `.band`, z-index 32 over the canvas, so it takes focus and shows its ring). The stage sets it over the sun each time the band is re-cut (`placeSky`: `band.project(aspect, panel)` projects the body through the panel's camera; at least 48px; moved only when it moved by more than a pixel; the band's top border (`clientTop`) is read in `measure`, not in the write phase). A tap on it does not splat the band's ink. The date line has the same switch on every sheet (`.dateline__night`, an 18px `#sun`/`#moon` symbol, 26px to press, 44px under a finger), so a sheet without the engraving can go back to day. src/ui/night.js toggles `:root.is-night`, `aria-pressed`, the icon, `meta[name=theme-color]` (#16140F) and `color-scheme`, keeps the choice in localStorage (`sd-night`) and emits `night`. partials/head.html (and 404.html, which has its own head) set the class before the first paint, so a night reader never sees a light flash; `state.night` reads the class.
- The engraving (src/gl/band.js): a crescent moon (a `THREE.Shape` of two arcs, a paper face lined at mode 0 and an ink edge a size larger, turned -0.55 rad), `skyK` 0..1 over 0.8s: the sun shrinks away (rays and all), then the moon grows. It stands in the sun's place, or at night high over the sea (it is the switch after dark in either edition). Both bodies obey the one-panel rule on narrow screens. `revealSky()` shows both for the warm-up, so the moon's program links under the intro; `morphing` keeps the band redrawing while it turns.
- Colours: `:root.is-night` (paper #16140F, aged #26221B, ink #ECE3CE, fade #9C927F), placed before `:root.is-proof` so the proof keeps its paper; the root's 0.8s cross-fade of registered properties carries it. The stage cross-fades the drawn inks with it (`INKS`, paper #B4AA95 and red #8A271D at night, smoothstep), forcing every canvas to redraw while they change. The red EXTRA sheet pins the day inks, and needs `color: var(--ink)` of its own: `body`'s colour is inherited as a computed colour, so a local `--ink` alone left its email and lede light on red. The contact card's QR stays dark on light. Print resets the night tokens.
- The date line with the new switch: the Italian date ("venerdì 25 settembre 2026") wrapped at 768-800px: the controls close to 12px gaps up to 1100px, and at 768-800px the date takes 1.6fr of the line and the date and clock cells pad 10px. Swept every 3-4px from 320 to 1440, three languages: every cell on one line, the controls apart.

Checked: desktop click and phone tap on the sun (sun to moon, the site dark, the caption and the date-line icon follow), the contact page opens dark and its date-line moon brings the day back (stored choice cleared), real night (?sun=22:00): the moon over the sea is the switch, a reload is dark before any script runs; axe with the night edition on (front page EN desktop and IT phone, contact, a case page in Albanian, the colophon on a phone): only the known fan finding, colour contrast included; `npm run test:admin` 72/72.

Trap: `npm run test:admin` against a worker started before the last build: the worker answered every page with an empty 404, the suite crashed after saving its test settings and never put them back. Restart the worker after every build before the suite, and if a run dies, put the settings back (scratch restore: `PUT /api/admin/settings` with `defaults()` from shared/settings.js).

## The poster press (2026-09-25)

Luca: "somthing new" once more. Picked and built: on the colophon, the line a visitor sets ("Set your own line", their business name) can be printed as a poster to save or share. Every shared poster carries Stefano's mark, name, trade and a code to his site. Local only, not committed or deployed.

- src/ui/poster.js draws it on a canvas, 1080 x 1350 (a phone's feed shows it whole), JPEG about 420 KB: the newsprint with paper.js's mottle and grain, a double-rule dateline (`albaniaNow` in the page's language, `T.zone`), the line upper-cased per language and fitted to the 936px measure in one, two or three lines (every break tried; the stack that prints its smallest line largest once scaled into its room), `/band-poster.jpg` between a 3px rule and a 10px ground, `T.posterLine`, and a foot with the page's own `#sd-mark` symbol (serialised to an SVG image), STEFANO DOKO, `T.role`, the host, and a QR (qr.js `qrCells`, EC M, 4-module quiet zone) to the front page in that language. Anybody's width: `condensed` in the font shorthand and `ctx.fontStretch` where the canvas has it; a canvas with neither sets the line wider and smaller, still fitted. Loaded only when the press is pulled (dynamic import).
- src/ui/setline.js: the press judders the line, sounds, shows "The press is running." in a `role=status` line, then the poster in a plate mount (`@starting-style` rise) with Save (a download link, `broadside-<slug>.jpg`, accents stripped with `\p{M}`) and Share (`navigator.share({ files })`, shown only where `canShare` says files can go). `ScrollTrigger.refresh()` the first time the sheet grows by it.
- Found on the way, fixed: the colophon's fitted line cut off Ë's dots and Ç's cedilla (a clipping box with line-height 0.84): 0.13em above and 0.14em below, with the margins reduced by the same, so the line sits where it did. And axe with the pill on screen (earlier runs were at the top, where it is hidden): `landmark-unique` (the pill's nav and the toolbar's were both "Sections") and `region` (the pill's mark and button outside a landmark). The pill is now itself the `<nav>`, "Sections (pinned bar)" in three languages (`pillLabel`); its inner nav is a div. Nothing selected `nav.pill__nav`; the pill measures the same (637px EN, 621px IT).

Checked: posters for short, long, Albanian and Italian names (desktop click, phone tap), each saved as a file and looked at; status, alt and file name in each language; the page with the poster at 1440, 820 and 390, day and night: nothing past the measure, no button text on two lines; axe on the colophon with the poster shown (EN desktop, IT phone, SQ phone at night, EN tablet, IT desktop at night) and on six sheets with the pill on: none; `npm run test:admin` 72/72, settings clean; no console errors.

## The front page's parts as pages of their own (2026-09-25)

Luca: "make every click a sub menu, the homepage will be how it is, but if people click something it should redirect". The menus and every link on the front page used to scroll down the one sheet; now they lead to pages, and the front page is otherwise unchanged. Local only, not committed or deployed.

- New pages, in three languages: /work/ (/sq/puna/, /it/lavori/: the fan, the three case sheets, graphic work and readers' letters when on), /about/ (/sq/rreth/, /it/chi-e/: the verse, the portrait when on), /services/ (/sq/sherbimet/, /it/servizi/: the three columns, the type case, the rates and the calculator when on). Each ends with the next sheet: work → about → websites → telegraph counter. src/routes.js, vite.config.js, i18n (wk/ab/sv Title, Description, Next; the descriptions reuse the site's reviewed sentences), og previews (tools/og.mjs, the work's with the fan's three phones; origin notes embedded, scan 0 missing).
- No copy of any part: the front page's parts moved word for word into partials/sheet-{work,graphic,quotes,about,services,rates}.html (a scratch script cut them by line and checked each boundary). The include plugin reads `lead` after a partial's name and fills its markers: `{{LEAD_CLASS}}` (sheet--lead: no top room), `{{LEAD_TOP}}` (the dateline), `{{H}}` (h1), `{{SUB}}` (column heads h2), `{{HEAD_CLASS}}` (sheethead--page), `{{HEAD_SOURCE}}` (inked by the intro). Without `lead` the markers are empty or the old levels: the built front page came out byte for byte the same in three languages before the links changed, and after, 48 lines differ, every one a link.
- Links: the toolbar, pill and tab bar words (`data-route`, and `data-spy="#part"` for the front page's reading marks), the index entries (routes in `T.menu`), the contents, the feature, "See the work", the fan's cards (Elixir and Bar Martiri to their case editions, Greta and graphic work to `/work/#greta` / `#graphic`), the case pages' "Back to the work". Only "Front page" and "Amusements" in the index stay on the front page. toolbar.js spies only on the front page, by `data-spy`, so a page's own `aria-current="page"` is never taken off; `.word[aria-current='page']` is inked now (it never was, the contact page's word included).
- Structured data: /work/ is a CollectionPage, /about/ an AboutPage; a case sheet's trail runs home → the work → the case.

Checked: every click from the front page on a desktop (toolbar, pill, contents, feature, "See the work", the fan's front card) and a phone (tab bar, index) lands on its page; each new page in three languages has one h1, its dateline, its parts, the right next link, its word marked and its canonical; the About verse takes ink to its last word before the foot (desktop, phone, 1920); axe on the new pages, desktop and phone, the pill on screen: none (the websites page's column heads were h3 under the h1 until `{{SUB}}`); `npm run test:admin` 78/78 (new: the pages are served with the settings and one head, graphic work reaches /sq/puna/, the trail, the front page's words lead to pages), settings clean; no console errors.

## The website checklist (2026-09-25)

Luca pasted a checklist of what a site should have (privacy policy, terms, FAQ, sitemap, 404, alt text, analytics, meta, sharing, favicon, canonical, cookie consent, mobile, forms, search, menus, loading, hover, progress bar, copy, print, sticky header, skip link, password eye, UTM, form success rate, error states, confirmation dialogs, last-updated date, floating contact). Each was checked; what was missing was built. Local only, not committed or deployed.

- Already there: the calls to write, robots.txt, alt text, titles and descriptions, link previews, the favicon set, canonicals, the phone menus, the opening counter, hover states, the print edition, the pinned bar and the tab bar (sticky header, floating contact), the skip link, "Copy the address", the form's error states.
- Built: /privacy/ and /terms/ in three languages (plain text; to be read by a native speaker or a lawyer before launch; the privacy page changes with what is switched on: visit counting), linked from every foot; six questions on the contact page (`<details>`, the + turns to ×; facts only, the price answer follows the rates part); share and "Updated <date>" in every foot; a search in the index (desktop and tablet); the phone's reading bar under the tab bar (a CSS scroll timeline, no script); UTM names noted and cleared from the address (src/ui/from.js) and carried by the telegram ("Came via" in the admin); the form's numbers, begun and sent per month (KV `stat:tg:<started|sent>:<month>`, Albania's months, counted only while visit counting is live; `POST /api/telegram/started`, 20 an hour per address); the admin's password eye and a question before the portrait goes (`<dialog>`, focus on "Keep it", Escape keeps); the 404 lists the pages.
- Not built: a cookie banner (the site sets no cookies for its visitors: a banner would say something untrue; the privacy page says so). The sitemap and absolute canonicals wait for the domain (`SITE_URL` at build).
- Checked: scratch checklist-public (privacy and terms in three languages, the questions, share, the date in each language, search in three languages with "nothing found" and Enter, the 404's pages, the reading bar) and checklist-worker (the empty form's errors, a sent telegram carrying "instagram / story", the admin's "Came via" and numbers, the question before the portrait goes, the password eye): all pass; `npm run test:admin` 84/84; settings, inbox and numbers clean.

## Phones, tablets and a phone held sideways (2026-09-25)

Luca: "optimize everything for mobile spacing and other". Measured before changing anything: a scratch audit of the 30 sheets and the 404 at 320, 375, 390 and 430px (the English also at 844 × 390, a phone held sideways; 115 runs) and at 768 × 1024, 820 × 1180, 1024 × 768 and 1180 × 820 (74 runs): what the page edge cuts off (html clips sideways, so it is just gone), targets under 24 and 44px, fields under 16px (iOS zooms), text under 12px, text within 12px of the screen's edge, text over text, holes over 30% of a screen, the last line against the tab bar, each sheet's spacing; and whole-page shots at 390, screen-by-screen shots sideways. Local only, not committed or deployed.

- Rhythm on phones: the sheets opened 72 to 88px under the last and closed 40 to 80px after their last lines, so the breaks ran from 88 to 168px (under the verse, 236px to the next head). Now every sheet opens 56px and closes 48px (the running line 48px down, the next sheet's name 56px, the foot 48px); sideways 64/56 instead of the desktop's 96 to 136px.
- The phone deck of proofs hung up to 150px under its 96vw box, over its hands and count, and left up to 180px of bare paper above. Hit-tested what shows on screen for each proof in front, three languages, 320 to 430px (scratch fan-visible): the box is `calc(152px + 45.5vw)` and the proofs stand on a floor 40px up it. Its captions went from 10 to 11px. The undealt pile (the fan still below the screen) is the only place axe finds a proof too small to press; dealt on screen, none.
- The foot's pages wrapped on 24px lines under 44px hit areas (a finger on one word reached the words above and below): two to a row, each on its own 44px line; the foot's other lines 20px apart.
- The measured table on "How it was made" ran four words to a line beside the figures, and at 320px "73, from about 370" went past the screen's edge: on phones each figure stands under its description (the table is `display: block`, each row a grid; the last row drops its rule under the table's own).
- Running text on phones at one size, 18px (the colophon's articles and privacy and terms were 19, the list of what a site does 19, the card line 19; letters to the editor 21 → 19; the verse `clamp(18px, 5vw, 21px)`). Two paragraphs under one head now have 0.7em between them (privacy's "A telegram" ran on).
- The lockup's descriptor 9.5 → 10.5px on phones (10px under 341px, where the Italian needs it), and the date line's date and clock 11 → 12px: scratch type-room and role-probe tried each size in three languages at 320 to 430px; the other session's `top.mjs` found nothing new at 320 to 430px (only its three known harmless lines).
- Buttons on phones may take a second line: the Italian "Manda il telegramma" at 320px ran 24px past the screen, "Stampala come manifesto" 11px.
- iPhones: the page kept 86px for the tab bar, but on an iPhone with a home bar the bar stands 34px up, so the last 8px of a page sat under it: the room is now `max(10px, safe-area) + 76px`, and the index rises 14px over the bar wherever it stands. The side margin is `max(clamp(16px, 3.2vw, 48px), safe-area-inset-left, -right)`: held sideways, viewport-fit=cover had let the words run under the notch.
- Held sideways (a touch screen ≥768px wide and <560px tall, `SHORT` in src/state.js and style.css): a held case sheet is at least 680px tall, so on a 390px screen its plates were never seen (screen shots: the head, then the next sheet). The sheets now lie one after another; cases.js drives the same choreography (plates develop, rules draw, facts are dealt) by the body's pass from 85% to 60% of the screen; ink.js inks the case names by view; main.js reloads when `short` changes, as it does for `mobile`. The pill is 50px at 8px, the index scrolls inside the screen without its search.
- Tablets: the red sheet's line had 0px beside its three buttons at 768 and 820px (upright iPads), 12px sideways and 140-180px at 1024; up to 1250px it stands over them (532px, its 34ch). The case editions' masthead at 768px: Ultra has no narrower cut, and at its 44px floor "Bar Martiri" needs 367px, 31px more than its column, so it ran 9px into the lines beside it; from 768 to 819px the manicule goes and its room passes to the name (22px clear at 768, 800, 819). The lede beside its buttons ran 20 to 29 letters a line: over them up to 1100px. The colophon at length runs one column up to 900px (26 letters a line in two). The amusements stand one under the other up to 1100px, and the letter box over its words wherever its column is under 480px (a container query): beside the game, the letter's column was 37px sideways and its buttons ran off the screen. The language keys are 44px with their frame under a finger.
- Admin on a phone: its words, keys, switches and section tabs are pressed on 44px under a finger, and the currency select is 16px (an iPhone zoomed on it at 15px).
- tools/test-admin.mjs: a step that throws halfway no longer leaves test data. It twice did today: the Worker takes 6 telegrams an hour from one address, the third run in the hour was refused its telegram, the script fell over on the missing item and left its test settings (a test email, a fake number, every part on) and a begun count. Now an uncaughtException handler puts the settings and numbers found at the start back and deletes the test telegram; a 429 says what it is. Tested by running into the limit on purpose: it stopped after 69 checks and put everything back.
- Checked after (the weather pass below re-ran the suite at 86/86): phones 115 runs: nothing cut off, no field under 16px, no text within 12px of the edge, holes 18 → 3 (the 404's pied type, meant empty, and one sideways sheet break); tablets 74 runs: nothing cut off, no narrow text; axe on the front page, contact (EN, IT), how it was made, privacy and Bar Martiri at 390, 844 × 390 and 768: none but the undealt pile above; scratch checklist-public all pass; `npm run test:admin` 84/84 on the final build (the local telegram count cleared with `wrangler kv key delete --local`); settings, inbox and numbers clean. Not tried on a real iPhone or Android from here: the safe-area rules can only be seen on one.

## The weather over Albania in the engraving (2026-09-25)

Luca: "somthing new" again (after the mobile pass); picked and built, as the sun that follows the real sun and the night edition were: the engraving's sky now takes the weather over the middle of Albania. Local only, not committed or deployed.

- Data: MET Norway's locationforecast/2.0/compact for 41.15, 20.17 (the point src/sun.js reckons from; the site names no city). Chosen over Open-Meteo, whose free tier is non-commercial only; MET's terms are an identifying User-Agent (app + contact: the public repo's address), caching, and credit. `GET /api/weather` (worker/public-api.js) asks at most every 30 minutes per data centre (Cache API, `max-age=1800`; five minutes after a failure) and answers `{ok, code, temp, wind, from, at}`; a visitor's browser asks only the site, so the privacy page's promise holds, and it now says where the weather comes from (pvHost, three languages). src/weather.js reads the symbol (`lightrainshowers_day` → rain, light; `…andthunder` → thunder; MET's own "lightssleet" spelling included) into sky, fall, amount, thunder, fog, wind and the caption's word; `?weather=`/`&wind=` set it by hand.
- Drawing (src/gl/band.js `setWeather`; DESIGN.md "The weather over Albania"): six more clouds for partly cloudy and overcast and a cloud across the sun or moon; rain and snow as GPU-placed strokes and paper flakes (one mesh each, positions from a seed and the time: nothing written per frame; a share shown per amount); fog as a per-material `uFog` (engrave.js: the distance fades to paper, far outlines dithered in the hull shader); a bolt with a fork at three open spots over the far sea (the first spots were behind the bottles and the cone: moved after the screenshots); wind in the umbrellas, the palm, the sea and the clouds' drift. The fog uniform is the engraving's own: it was first put in `shared`, which the brayer's materials also read, and a fog would have faded the roller.
- Caption (toolbar.js): the sun's line, then "Its sky is Albania's too: light rain, 15°C (MET Norway).", then what pressing the body does; "How it was made" says it too. Checked live against MET's reading of the moment (light rain showers, 15°C, 1.7 m/s from the north) in three languages, desktop and phone.
- Found on the way, fixed: the red sheet's stamp was turned by a script write every frame (a full compositor update each frame it was on screen); on a phone at a quarter speed it dropped every other frame there (redsheet.mjs: 28 and 47 frames over 20ms in two first-visit runs, layerize about 1s). It is now a WAAPI rotation whose rate follows the scroll's pace in half steps, paused off screen and with the press stopped: 8 and 10 frames, layerize halved; the full phone profile (CPU x4, storm on) is p95 16.8ms in every stretch (the red sheet's stretch had 10 frames over 20ms, now 1), desktop 16.8 with none over 20ms.
- Checked: stills of every weather (clear, partly, overcast, fog, light and heavy rain with wind, thunder, snow, sleet, rain at night) and the bolt caught mid-flash (`window.__sdBand.debugWeather()` in development); the WebGL console clean at 390, 844 × 390 and 820 × 1180 with a storm; `npm run test:admin` 86/86 (the weather is a MET symbol and a temperature or no reading, and it is cached); settings, inbox and numbers clean; the three languages' keys match.
- Then Luca asked whether it follows Tirana: it read the middle of the country (41.15, 20.17), and at that moment the two differed (showers and 15°C there, clear and 18°C in Tirana). The Worker now reads Tirana (41.3275, 19.8187; cache key tirana-v2) and takes the hour under way: MET's list can begin an hour or two back, and the first reading after the switch was an hour old. The sun is still reckoned from the middle (a minute or two from Tirana's), and the caption still says "Albania's": the site names no city for Stefano. test:admin 86/86, store clean.

## The modern edition, the road, and Tirana by name (2026-09-25)

Luca: "optimize it for iphone and android and make the website when scrolling to make a red path for the car that it will follow this path both Pc and mobile and optimize it and make it say tirana on the wether and make the look more modern ... after u finish push on git hub all the work we did today". Built in that order, verified locally, then committed and pushed (not deployed).

- **Tirana.** The caption now says "Its sky is Tirana's right now: partly cloudy, 16°C (MET Norway)." (SQ "Qielli i gravurës është tani ai i Tiranës", IT "Il cielo dell'incisione è quello di Tirana adesso"); "How it was made" and the privacy note say Tirana too, in three languages. It names where the weather is read, not where Stefano lives.
- **The modern edition** (DESIGN.md, "The modern edition", which wins over the older sections): reading text in Inter (the brand's face, the Latin subset from brand/v5), upright everywhere, a size smaller than Old Standard took for the same colour on the page; the fat face gone: Anybody 900 set wide (`--fat-w` 118%, fit.js `FAT_W`); one hairline where there were double rules; pills for controls, 20px for plates and cards, 26px for the blank and the tariff; case sheets rise as cards with rounded tops; the engraving band a rounded cut on wider screens; the star an asterisk (red before column heads), the manicule an arrow (same symbol ids); no mast hand, stars or vertical rules, no fleurons, no speckle on heads; a finer grain and no mottle (paper.js stops making it: less work at boot); the pinned bar and the phone's tab bar frosted. The WebGL band and plates round their pictures to the same corners (stage.js and plates.js `uRadius`, premultiplied output on a cleared canvas). The admin reads in Inter too. Link previews re-rendered in the new type (tools/og.mjs; 24 pictures, origin notes embedded, `--scan` 91 rasters, 0 missing). Ultra and Old Standard stay in public/fonts for the brand set's templates only. The `.impeccable/design.json` sidecar predates this: regenerate it with `/impeccable document` when wanted.
- **The road** (src/ui/road.js; DESIGN.md, Components, "The road"): the Elixir van, seen from above, drives down every sheet at a reading line and paints its road red; the way ahead is dotted. It crosses the page only in clear strips, found from every line of words on the page (Range rects over the text nodes) plus pictures, controls and blocks; the first version looked at elements only, and on a phone a crossing ran through a case key's labels (spans). Case sheets held on screen in turn are one block (desktop, tablet); a phone's are not, so its road winds between them. The running line is a bridge the road passes under (z-index 34 and its own paper). Scroll-to-height is a base (wait under the start or catch up with the reading line, keep to it, hurry over the last half screen if the scroll ends first) with a slow stretch over each crossing and a catch-up after it; all of it becomes keyframes on a ScrollTimeline (van: translate + rotate; the paint: a clip box and its counter-move, the ink window's trick), so it runs on the compositor with the scroll; the one clock moves it where there is no ScrollTimeline. Rebuilt on any body resize, refit, language or still. Front page: 3 crossings at 1440, 4 at 390; 0 on the work page at desktop sizes (the stack is one block), 3 on phones.
- **iPhone and Android:** `format-detection` off (no numbers turned into phone links), `color-scheme: only light` (Android's forced dark would repaint the day's sheet; night.js keeps "only light" when it switches back), no grey tap flash and no double-tap wait on anything pressed (`touch-action: manipulation`; the deck keeps pan-y by intersection), the tab bar and the van step aside while a text field has the keyboard (CSS `:has()`), the address bar sliding in and out no longer re-fits, re-measures and refreshes every trigger (main.js: a touch screen's height-only change under 160px just updates `state.vh`), the contact form's keyboards say next/send/done and the reply field is not autocorrected or capitalised, frosted bars fall back to paper under `prefers-reduced-transparency`.
- **Performance traps met.** (1) Rounded case sheets with `overflow: hidden` clipped every composited thing on them by a rounded mask while the stack slid: p95 33ms and frames of 50ms over the cases on the desktop profile; the paper, rule, corners and shadow moved to `.case::before` behind a sheet whose clip stays rectangular: back to 16.7/16.8 with none over 20ms. (2) The pinned bar's backdrop blur cost one ~100ms frame with an idle main thread the first time it slid in (GPU programs compiled): warm.js now draws a copy of the bar under the intro. After both, desktop profile: every stretch p95 16.7-16.8, no long animation frames; phone at a quarter CPU: p95 16.7-16.8, 3 frames over 20ms in the whole run (as the baseline), none long.
- **Checked:** 130 page/size runs (10 sheets × 13 sizes from 320×568 to 1920×1080, sideways phone included): no sideways scroll, no clipped control words, the road built with its animations everywhere (the only findings were the ink windows and the undealt facts, off screen by design); crossings looked at frame by frame on phone and desktop, all in clear strips; axe (WCAG 2.2 AA + best practice) on 12 page/size/language runs: none but the phone deck's undealt pile (known); night, tablet, sideways phone, the phone's index and language sheet looked at; reduced motion, Stop the press and print (road whole, no van; nothing printed); the keyboard check (tab bar gone while typing, back after); `npm run test:admin` 86/86 with Tirana's reading live. Not tried on a real iPhone or Android from here.
